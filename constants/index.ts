// Barrel file – importers can simply write:
//   import { IMAGES, CAROUSEL_IMAGES, CONTENT } from "@/constants"
// Keep paths **without** a file-extension so the next-lite resolver finds the
// original TypeScript modules.
export * from "./images"
export * from "./content"
